<beans:beans xmlns="http://www.springframework.org/schema/security"
	xmlns:beans="http://www.springframework.org/schema/beans" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:security="http://www.springframework.org/schema/security"
	xsi:schemaLocation="http://www.springframework.org/schema/beans	http://www.springframework.org/schema/beans/spring-beans-4.2.xsd
	http://www.springframework.org/schema/security http://www.springframework.org/schema/security/spring-security-4.1.xsd">

	<security:http auto-config="true"  >
	<security:csrf disabled="true"/>
		<security:intercept-url pattern="/" access="permitAll" />
		<security:intercept-url pattern="/login" access="permitAll" />
		<security:intercept-url pattern="/cadastroUsuarios" access="permitAll" />
		<security:intercept-url pattern="/welcome" access="permitAll" />
		<security:intercept-url pattern="/index" access="hasAnyRole('ROLE_Administrador', 'ROLE_Usuario')" />
		<security:intercept-url pattern="/relatorios" access="hasAnyRole('ROLE_Administrador')" />
		
		<security:form-login 
		    login-page="/login"
		    default-target-url="/index" 
			authentication-failure-url="/login?error" 
			username-parameter="username"
			password-parameter="password" />
		<security:logout logout-success-url="/login?logout" />
	</security:http> 

	<security:authentication-manager>
	  <security:authentication-provider>
	    <security:jdbc-user-service data-source-ref="controleHoras"
			users-by-username-query="select user as username, password, 1 as enable from users where user=?"
			authorities-by-username-query="select usr.user as username, CONCAT('ROLE_',usr.userRole) as authority from userroles usr where usr.user = ?" />
	  </security:authentication-provider>
	</security:authentication-manager>
</beans:beans>
